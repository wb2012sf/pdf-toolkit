{
  // Typecheck sources, tests and the vite config. No rootDir: it is enforced
  // even with noEmit, and the engine's sources sit outside this package.
  "extends": "./tsconfig.json",
  "include": ["src", "test", "vite.config.ts"]
}
